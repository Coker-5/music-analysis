# 音乐榜单可视化平台 — MySQL 数据库建表文档

## 项目背景

基于爬虫技术的音乐榜单多维度演化分析与可视化平台。

数据来源：
- 腾讯音乐周榜历史数据（2018–2025年）
- 腾讯音乐日榜数据（每日爬虫抓取）

---

## 表结构总览（共 8 张表）

| 类型 | 表名 | 用途 |
|------|------|------|
| 维度表 | `dim_singer` | 歌手基础信息 |
| 维度表 | `dim_week_issue` | 周榜期数元数据 |
| 事实表 | `fact_weekly_chart` | 历史周榜明细（总表） |
| 指数表 | `fact_weekly_classify_index` | 周榜五维分项指数 |
| 事实表 | `fact_daily_chart` | 日榜明细（当日表，每日爬虫写入） |
| 聚合表 | `agg_singer_yearly` | 歌手历年上榜统计（支撑演化分析） |
| 聚合表 | `agg_song_longevity` | 歌曲留榜生命力统计 |
| 聚合表 | `agg_singer_total` | 歌手全量上榜统计 |

---

## 建表 SQL

```sql
-- ================================================================
-- 音乐榜单可视化平台 MySQL 数据库建表 SQL
-- 架构：2张维度表 + 2张事实表 + 1张指数表 + 3张聚合表
-- ================================================================

-- ----------------------------------------------------------------
-- 【维度表1】歌手维度表
-- 对应图表：①歌手占比饼图 ②词云图 ④条形图 ⑦留榜表 ⑨3D柱状图
-- ----------------------------------------------------------------
CREATE TABLE dim_singer (
    singer_id     VARCHAR(20)  PRIMARY KEY                    COMMENT '歌手ID（来源平台）',
    singer_name   VARCHAR(100) NOT NULL                       COMMENT '歌手名',
    created_at    DATETIME     DEFAULT CURRENT_TIMESTAMP
) COMMENT '歌手维度表';


-- ----------------------------------------------------------------
-- 【维度表2】周榜期数元数据表
-- 对应图表：用于关联历史周榜的期数与时间，支撑时序分析
-- 数据来源：years_issue_data.json
-- ----------------------------------------------------------------
CREATE TABLE dim_week_issue (
    issue         VARCHAR(10)  PRIMARY KEY  COMMENT '期数，如 202552',
    year          SMALLINT     NOT NULL     COMMENT '年份',
    month         TINYINT      NOT NULL     COMMENT '月份',
    start_date    DATE         NOT NULL     COMMENT '本期开始日期',
    end_date      DATE         NOT NULL     COMMENT '本期结束日期',
    title         VARCHAR(100)              COMMENT '期数标题',
    publish_time  DATETIME                  COMMENT '发布时间'
) COMMENT '周榜期数元数据表';


-- ----------------------------------------------------------------
-- 【事实表1】历史周榜明细表（总表）
-- 对应图表：①饼图 ②词云 ④条形图 ⑦留榜表 ⑨3D柱状图
-- 数据来源：example_music.json（2018–2025年全量周榜）
-- ----------------------------------------------------------------
CREATE TABLE fact_weekly_chart (
    id               BIGINT       PRIMARY KEY AUTO_INCREMENT,
    issue            VARCHAR(10)  NOT NULL                    COMMENT '期数，关联 dim_week_issue',
    year             SMALLINT     NOT NULL                    COMMENT '年份（冗余，加速聚合查询）',
    rank             TINYINT      NOT NULL                    COMMENT '本期排名',
    last_week_rank   TINYINT                                  COMMENT '上期排名，0 表示新入榜',
    song_id          VARCHAR(20)  NOT NULL                    COMMENT '歌曲ID',
    song_name        VARCHAR(200) NOT NULL                    COMMENT '歌曲名',
    singer_id        VARCHAR(20)  NOT NULL                    COMMENT '歌手ID，关联 dim_singer',
    singer_name      VARCHAR(100) NOT NULL                    COMMENT '歌手名（冗余）',
    uni_index        DECIMAL(6,2)                             COMMENT '综合指数',
    on_chart_weeks   SMALLINT                                 COMMENT '本歌曲累计上榜周数',
    highest_rank     TINYINT                                  COMMENT '本期最高排名',
    history_highest  TINYINT                                  COMMENT '历史最高排名（全周期）',
    new_flag         TINYINT(1)   DEFAULT 0                   COMMENT '是否新入榜',
    cover_image      VARCHAR(500)                             COMMENT '封面图 URL',
    INDEX idx_issue     (issue),
    INDEX idx_year      (year),
    INDEX idx_singer_id (singer_id),
    INDEX idx_song_id   (song_id)
) COMMENT '历史周榜明细表（总表）';


-- ----------------------------------------------------------------
-- 【指数表】周榜五维分项指数表
-- 对应图表：⑤三环仪表盘（周榜维度备用），与 fact_weekly_chart 1对多
-- 数据来源：classifyIndices 字段（播放热度/传播度/喜好度/畅销度/推荐度）
-- ----------------------------------------------------------------
CREATE TABLE fact_weekly_classify_index (
    id            BIGINT       PRIMARY KEY AUTO_INCREMENT,
    weekly_id     BIGINT       NOT NULL    COMMENT '关联 fact_weekly_chart.id',
    issue         VARCHAR(10)  NOT NULL    COMMENT '期数（冗余，便于查询）',
    song_id       VARCHAR(20)  NOT NULL    COMMENT '歌曲ID',
    index_code    TINYINT      NOT NULL    COMMENT '指数类型：1播放热度 2传播度 3喜好度 4畅销度 5推荐度',
    index_name    VARCHAR(20)  NOT NULL    COMMENT '指数名称',
    index_value   DECIMAL(6,2)             COMMENT '指数值',
    percentage    TINYINT                  COMMENT '权重占比（%）',
    is_champion   TINYINT(1)   DEFAULT 0   COMMENT '是否该维度冠军',
    INDEX idx_weekly_id  (weekly_id),
    INDEX idx_song_issue (song_id, issue)
) COMMENT '周榜五维分项指数表';


-- ----------------------------------------------------------------
-- 【事实表2】日榜明细表（当日表）
-- 对应图表：③今日数字卡片 ⑤三环仪表盘 ⑥得分区间分布 ⑧今日滚动榜单
-- 数据来源：latest_data.json（每日爬虫抓取写入）
-- ----------------------------------------------------------------
CREATE TABLE fact_daily_chart (
    id               BIGINT       PRIMARY KEY AUTO_INCREMENT,
    chart_date       DATE         NOT NULL                    COMMENT '榜单日期',
    issue            VARCHAR(10)  NOT NULL                    COMMENT '日榜期数，如 202667',
    rank             SMALLINT     NOT NULL                    COMMENT '今日排名',
    pre_rank         SMALLINT                                 COMMENT '昨日排名',
    incr_rank        SMALLINT                                 COMMENT '排名变化，正数为上升',
    song_id          VARCHAR(20)  NOT NULL                    COMMENT '歌曲ID',
    song_name        VARCHAR(200) NOT NULL                    COMMENT '歌曲名',
    singer_id        VARCHAR(20)                              COMMENT '歌手ID',
    singer_name      VARCHAR(100) NOT NULL                    COMMENT '歌手名',
    score            DECIMAL(6,2) NOT NULL                    COMMENT '今日综合得分',
    pre_score        DECIMAL(6,2)                             COMMENT '昨日得分',
    incr_score       DECIMAL(6,2)                             COMMENT '得分涨跌',
    play_index       DECIMAL(6,2)                             COMMENT '播放指数（playIndex）',
    pay_index        DECIMAL(6,2)                             COMMENT '畅销度指数（payIndex）',
    pop_index        DECIMAL(6,2)                             COMMENT '人气指数（popIndex）',
    days_on_chart    SMALLINT                                 COMMENT '累计上榜天数',
    high_rank        SMALLINT                                 COMMENT '历史最高排名',
    high_index       DECIMAL(6,2)                             COMMENT '历史最高得分',
    new_flag         TINYINT(1)   DEFAULT 0                   COMMENT '是否今日新入榜',
    first_on_chart   TINYINT(1)   DEFAULT 0                   COMMENT '是否首次上榜',
    track_tags       JSON                                     COMMENT '标签数组，如 ["五连冠","连续19天TOP10"]',
    cover_image      VARCHAR(500)                             COMMENT '封面图 URL',
    publish_time     DATETIME                                 COMMENT '歌曲发行时间',
    created_at       DATETIME     DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_date_rank  (chart_date, `rank`),
    INDEX idx_chart_date     (chart_date),
    INDEX idx_singer_date    (singer_id, chart_date),
    INDEX idx_score          (chart_date, score)
) COMMENT '日榜明细表（当日表，每日爬虫写入）';


-- ----------------------------------------------------------------
-- 【聚合表1】歌手历年上榜统计表
-- 对应图表：⑨历年演化3D柱状图（核心图表）、④横向条形图
-- 更新策略：每次周榜爬虫写入后，定时脚本刷新
-- ----------------------------------------------------------------
CREATE TABLE agg_singer_yearly (
    id              BIGINT       PRIMARY KEY AUTO_INCREMENT,
    singer_id       VARCHAR(20)  NOT NULL    COMMENT '歌手ID',
    singer_name     VARCHAR(100) NOT NULL    COMMENT '歌手名',
    year            SMALLINT     NOT NULL    COMMENT '年份',
    chart_count     SMALLINT     DEFAULT 0   COMMENT '当年上榜总周次',
    top3_count      SMALLINT     DEFAULT 0   COMMENT '当年进入 Top3 次数',
    champion_count  SMALLINT     DEFAULT 0   COMMENT '当年夺冠次数',
    avg_rank        DECIMAL(5,2)             COMMENT '当年平均排名',
    avg_index       DECIMAL(6,2)             COMMENT '当年平均综合指数',
    updated_at      DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_singer_year (singer_id, year),
    INDEX idx_year (year)
) COMMENT '歌手历年上榜统计聚合表（支撑演化分析）';


-- ----------------------------------------------------------------
-- 【聚合表2】歌曲留榜生命力统计表
-- 对应图表：⑦历史长红歌曲留榜排行
-- 更新策略：每次周榜爬虫写入后，定时脚本刷新
-- ----------------------------------------------------------------
CREATE TABLE agg_song_longevity (
    id                BIGINT       PRIMARY KEY AUTO_INCREMENT,
    song_id           VARCHAR(20)  NOT NULL UNIQUE COMMENT '歌曲ID',
    song_name         VARCHAR(200) NOT NULL         COMMENT '歌曲名',
    singer_id         VARCHAR(20)  NOT NULL         COMMENT '歌手ID',
    singer_name       VARCHAR(100) NOT NULL         COMMENT '歌手名',
    total_weeks       SMALLINT     DEFAULT 0        COMMENT '累计上榜总周数',
    history_best_rank TINYINT                       COMMENT '历史最高排名',
    first_chart_date  DATE                          COMMENT '首次上榜日期',
    last_chart_date   DATE                          COMMENT '最近上榜日期',
    champion_weeks    SMALLINT     DEFAULT 0        COMMENT '夺冠周数',
    cover_image       VARCHAR(500)                  COMMENT '封面图 URL',
    updated_at        DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_total_weeks (total_weeks DESC),
    INDEX idx_singer_id   (singer_id)
) COMMENT '歌曲留榜生命力统计表';


-- ----------------------------------------------------------------
-- 【聚合表3】歌手全量上榜统计表
-- 对应图表：①歌手占比饼图 ②词云图 ④横向条形图
-- 更新策略：每次周榜爬虫写入后，定时脚本刷新
-- ----------------------------------------------------------------
CREATE TABLE agg_singer_total (
    singer_id         VARCHAR(20)  PRIMARY KEY      COMMENT '歌手ID',
    singer_name       VARCHAR(100) NOT NULL          COMMENT '歌手名',
    total_count       INT          DEFAULT 0         COMMENT '历史累计上榜总周次',
    champion_count    SMALLINT     DEFAULT 0         COMMENT '历史夺冠次数',
    top3_count        SMALLINT     DEFAULT 0         COMMENT '历史进入 Top3 次数',
    distinct_songs    SMALLINT     DEFAULT 0         COMMENT '上榜不重复歌曲数',
    best_rank         TINYINT                        COMMENT '历史最好排名',
    active_years      TINYINT                        COMMENT '活跃年份数',
    first_chart_year  SMALLINT                       COMMENT '首次上榜年份',
    last_chart_year   SMALLINT                       COMMENT '最近上榜年份',
    updated_at        DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_total_count (total_count DESC)
) COMMENT '歌手全量上榜统计聚合表（饼图/词云/条形图数据源）';
```

---

## 表关系说明

```
dim_singer ──────────────────────────────────────────────────┐
dim_week_issue ──────────────────┐                           │
                                 ↓                           ↓
                        fact_weekly_chart ──→ fact_weekly_classify_index
                                 │
                                 ↓（定期聚合脚本刷新）
                        agg_singer_yearly
                        agg_song_longevity
                        agg_singer_total

fact_daily_chart（独立，每日爬虫写入，不依赖历史周榜表）
```

---

## 各图表与数据表对应关系

| # | 图表名称 | 数据来源 | 查询目标表 |
|---|---------|---------|-----------|
| ① | 歌手上榜占比饼图 | 历史周榜 | `agg_singer_total` |
| ② | 歌手词云图 | 历史周榜 | `agg_singer_total` |
| ③ | 今日核心指标数字卡片 | 今日日榜 | `fact_daily_chart` |
| ④ | Top歌手上榜条形图 | 历史周榜 | `agg_singer_total` |
| ⑤ | 今日Top1三维指数环形图 | 今日日榜 | `fact_daily_chart` |
| ⑥ | 今日得分区间分布图 | 今日日榜 | `fact_daily_chart` |
| ⑦ | 长红歌曲留榜排行 | 历史周榜 | `agg_song_longevity` |
| ⑧ | 今日实时榜单滚动表 | 今日日榜 | `fact_daily_chart` |
| ⑨ | 歌手历年演化3D柱状图 | 历史周榜+年榜 | `agg_singer_yearly` |

---

## 聚合表更新策略

三张聚合表（`agg_*`）**无需实时计算**，建议在每次爬虫写入 `fact_weekly_chart` 后，通过定时脚本执行 `INSERT ... ON DUPLICATE KEY UPDATE` 进行刷新。前端查询时直接读聚合表，无需实时 GROUP BY，响应速度极快。