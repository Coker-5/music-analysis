# 音乐榜单可视化平台 — 完整操作实现文档

## 项目说明

本文档面向 AI 编码助手，描述从零开始完成数据库建表、爬虫启动、数据清洗、数据入库的全流程实现细节。

**项目根目录结构（目标）：**
```
music-analysis/
└── crawler/
    ├── config.py         ← 配置（数据库连接、路径常量）
    ├── models.py         ← 数据结构定义（dataclass）
    ├── spider.py         ← 爬虫（已完成，勿修改）
    ├── cleaner.py        ← 数据清洗（待实现）
    ├── db.py             ← 数据库操作封装（待实现）
    ├── pipeline.py       ← 清洗→入库流水线（待实现）
    ├── scheduler.py      ← 定时任务（待实现）
    ├── main.py           ← 统一入口（待实现）
    └── statics/
        ├── result_data.json       ← 历史周榜全量数据（已存在）
        ├── years_issue_data.json  ← 期数元数据（已存在）
        └── latest_data.json       ← 每日爬虫写入（每天覆盖）
```

**技术栈：**
- Python 3.10+
- MySQL 8.0+
- 依赖库：`DrissionPage`、`pymysql`、`APScheduler`

---

## 第一步：数据库建表

在 MySQL 中执行以下 SQL，按顺序执行，不可乱序。

```sql
-- ================================================================
-- 创建数据库
-- ================================================================
CREATE DATABASE IF NOT EXISTS music_chart
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

USE music_chart;

-- ================================================================
-- 【维度表1】歌手维度表
-- ================================================================
CREATE TABLE dim_singer (
    singer_id     VARCHAR(20)  PRIMARY KEY              COMMENT '歌手ID（腾讯音乐 singerId 字段）',
    singer_name   VARCHAR(100) NOT NULL                 COMMENT '歌手名',
    created_at    DATETIME     DEFAULT CURRENT_TIMESTAMP
) COMMENT '歌手维度表';


-- ================================================================
-- 【维度表2】周榜期数元数据表
-- 数据来源：years_issue_data.json 每个条目
-- 原始字段映射：
--   issue        → issue
--   month        → month
--   startTime    → start_date
--   endTime      → end_date
--   title        → title
--   publishTime  → publish_time
-- ================================================================
CREATE TABLE dim_week_issue (
    issue         VARCHAR(10)  PRIMARY KEY  COMMENT '期数编号，如 202552',
    year          SMALLINT     NOT NULL     COMMENT '年份（从 issue 前4位解析）',
    month         TINYINT      NOT NULL     COMMENT '月份',
    start_date    DATE         NOT NULL     COMMENT '本期开始日期',
    end_date      DATE         NOT NULL     COMMENT '本期结束日期',
    title         VARCHAR(100)              COMMENT '期数标题，如 第52期（12.29-01.04）',
    publish_time  DATETIME                  COMMENT '本期发布时间'
) COMMENT '周榜期数元数据表';


-- ================================================================
-- 【事实表1】历史周榜明细表（总表）
-- 数据来源：result_data.json，结构为 {year: {issue: [song, ...]}}
-- 原始字段映射：
--   issue（外层key）     → issue
--   year（外层key前4位） → year
--   rank                 → rank
--   lastWeekRank         → last_week_rank
--   songId               → song_id
--   songName             → song_name
--   singerId             → singer_id
--   singerName           → singer_name
--   uniIndex             → uni_index（字符串转 DECIMAL）
--   onChartWeeks         → on_chart_weeks
--   highestRank          → highest_rank
--   historyHighestRank   → history_highest
--   newFlag              → new_flag（bool 转 0/1）
--   coverImages          → cover_image
-- ================================================================
CREATE TABLE fact_weekly_chart (
    id               BIGINT       PRIMARY KEY AUTO_INCREMENT,
    issue            VARCHAR(10)  NOT NULL    COMMENT '期数，关联 dim_week_issue.issue',
    year             SMALLINT     NOT NULL    COMMENT '年份（冗余加速查询）',
    rank             TINYINT      NOT NULL    COMMENT '本期排名',
    last_week_rank   TINYINT                  COMMENT '上期排名，0 表示新入榜',
    song_id          VARCHAR(20)  NOT NULL    COMMENT '歌曲ID（songId）',
    song_name        VARCHAR(200) NOT NULL    COMMENT '歌曲名',
    singer_id        VARCHAR(20)  NOT NULL    COMMENT '歌手ID，关联 dim_singer.singer_id',
    singer_name      VARCHAR(100) NOT NULL    COMMENT '歌手名（冗余）',
    uni_index        DECIMAL(6,2)             COMMENT '综合指数',
    on_chart_weeks   SMALLINT                 COMMENT '累计上榜周数',
    highest_rank     TINYINT                  COMMENT '本期最高排名',
    history_highest  TINYINT                  COMMENT '历史最高排名',
    new_flag         TINYINT(1)   DEFAULT 0   COMMENT '是否新入榜（0/1）',
    cover_image      VARCHAR(500)             COMMENT '封面图 URL（coverImages 字段）',
    INDEX idx_issue     (issue),
    INDEX idx_year      (year),
    INDEX idx_singer_id (singer_id),
    INDEX idx_song_id   (song_id)
) COMMENT '历史周榜明细表';


-- ================================================================
-- 【指数表】周榜五维分项指数表
-- 数据来源：result_data.json 中每首歌的 classifyIndices 数组
-- 原始字段映射（classifyIndices 数组中每个对象）：
--   code        → index_code（1播放热度 2传播度 3喜好度 4畅销度 5推荐度）
--   name        → index_name
--   index       → index_value（字符串转 DECIMAL）
--   percentage  → percentage
--   isChampion  → is_champion（bool 转 0/1）
-- ================================================================
CREATE TABLE fact_weekly_classify_index (
    id            BIGINT       PRIMARY KEY AUTO_INCREMENT,
    weekly_id     BIGINT       NOT NULL    COMMENT '关联 fact_weekly_chart.id',
    issue         VARCHAR(10)  NOT NULL    COMMENT '期数（冗余）',
    song_id       VARCHAR(20)  NOT NULL    COMMENT '歌曲ID（冗余）',
    index_code    TINYINT      NOT NULL    COMMENT '指数类型编码',
    index_name    VARCHAR(20)  NOT NULL    COMMENT '指数名称',
    index_value   DECIMAL(6,2)             COMMENT '指数值',
    percentage    TINYINT                  COMMENT '权重占比（%）',
    is_champion   TINYINT(1)   DEFAULT 0   COMMENT '是否该维度冠军（0/1）',
    INDEX idx_weekly_id  (weekly_id),
    INDEX idx_song_issue (song_id, issue)
) COMMENT '周榜五维分项指数表';


-- ================================================================
-- 【事实表2】日榜明细表（当日表）
-- 数据来源：latest_data.json，每日爬虫覆盖写入
-- 原始字段映射：
--   issue                    → issue
--   rank                     → rank
--   preRank                  → pre_rank
--   incrRank                 → incr_rank
--   itemId                   → song_id
--   info.trackNameShow       → song_name
--   info.singerId            → singer_id（注意：用 info.singerId，非 info.qySingerId）
--   info.singerName          → singer_name
--   score                    → score（字符串转 DECIMAL）
--   preScore                 → pre_score
--   incrScore                → incr_score
--   extend.playIndex         → play_index
--   extend.payIndex          → pay_index
--   extend.popIndex          → pop_index
--   extend.days              → days_on_chart（字符串转 INT）
--   extend.highRank          → high_rank（字符串转 INT）
--   extend.highIndex         → high_index（字符串转 DECIMAL）
--   extend.newFlag           → new_flag（bool 转 0/1）
--   firstOnChart             → first_on_chart（0/1）
--   trackTags                → track_tags（list 转 JSON 字符串）
--   info.coverImages         → cover_image
--   info.publishTime         → publish_time（字符串转 DATETIME，可能为 null）
--   chart_date 需在入库时从当天日期 date.today() 生成
-- ================================================================
CREATE TABLE fact_daily_chart (
    id               BIGINT       PRIMARY KEY AUTO_INCREMENT,
    chart_date       DATE         NOT NULL                  COMMENT '榜单日期（爬取当天）',
    issue            VARCHAR(10)  NOT NULL                  COMMENT '日榜期数',
    rank             SMALLINT     NOT NULL                  COMMENT '今日排名',
    pre_rank         SMALLINT                               COMMENT '昨日排名',
    incr_rank        SMALLINT                               COMMENT '排名变化，正数上升',
    song_id          VARCHAR(20)  NOT NULL                  COMMENT '歌曲ID（itemId）',
    song_name        VARCHAR(200) NOT NULL                  COMMENT '歌曲名',
    singer_id        VARCHAR(20)                            COMMENT '歌手ID',
    singer_name      VARCHAR(100) NOT NULL                  COMMENT '歌手名',
    score            DECIMAL(6,2) NOT NULL                  COMMENT '今日综合得分',
    pre_score        DECIMAL(6,2)                           COMMENT '昨日得分',
    incr_score       DECIMAL(6,2)                           COMMENT '得分涨跌',
    play_index       DECIMAL(6,2)                           COMMENT '播放指数',
    pay_index        DECIMAL(6,2)                           COMMENT '畅销度指数',
    pop_index        DECIMAL(6,2)                           COMMENT '人气指数',
    days_on_chart    SMALLINT                               COMMENT '累计上榜天数',
    high_rank        SMALLINT                               COMMENT '历史最高排名',
    high_index       DECIMAL(6,2)                           COMMENT '历史最高得分',
    new_flag         TINYINT(1)   DEFAULT 0                 COMMENT '是否今日新入榜',
    first_on_chart   TINYINT(1)   DEFAULT 0                 COMMENT '是否首次上榜',
    track_tags       JSON                                   COMMENT '标签数组 JSON',
    cover_image      VARCHAR(500)                           COMMENT '封面图 URL',
    publish_time     DATETIME                               COMMENT '歌曲发行时间',
    created_at       DATETIME     DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_date_rank  (chart_date, `rank`),
    INDEX idx_chart_date     (chart_date),
    INDEX idx_singer_date    (singer_id, chart_date),
    INDEX idx_score          (chart_date, score)
) COMMENT '日榜明细表（每日爬虫写入）';


-- ================================================================
-- 【聚合表1】歌手历年上榜统计表
-- 由 fact_weekly_chart 聚合生成，支撑 ⑨ 历年演化3D柱状图
-- 不需要手动写数据，由 pipeline 脚本计算后写入
-- ================================================================
CREATE TABLE agg_singer_yearly (
    id              BIGINT       PRIMARY KEY AUTO_INCREMENT,
    singer_id       VARCHAR(20)  NOT NULL    COMMENT '歌手ID',
    singer_name     VARCHAR(100) NOT NULL    COMMENT '歌手名',
    year            SMALLINT     NOT NULL    COMMENT '年份',
    chart_count     SMALLINT     DEFAULT 0   COMMENT '当年上榜总周次',
    top3_count      SMALLINT     DEFAULT 0   COMMENT '当年进入 Top3 次数',
    champion_count  SMALLINT     DEFAULT 0   COMMENT '当年夺冠（rank=1）次数',
    avg_rank        DECIMAL(5,2)             COMMENT '当年平均排名',
    avg_index       DECIMAL(6,2)             COMMENT '当年平均综合指数',
    updated_at      DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_singer_year (singer_id, year),
    INDEX idx_year (year)
) COMMENT '歌手历年上榜统计聚合表';


-- ================================================================
-- 【聚合表2】歌曲留榜生命力统计表
-- 由 fact_weekly_chart 聚合生成，支撑 ⑦ 长红歌曲排行
-- ================================================================
CREATE TABLE agg_song_longevity (
    id                BIGINT       PRIMARY KEY AUTO_INCREMENT,
    song_id           VARCHAR(20)  NOT NULL UNIQUE COMMENT '歌曲ID',
    song_name         VARCHAR(200) NOT NULL         COMMENT '歌曲名',
    singer_id         VARCHAR(20)  NOT NULL         COMMENT '歌手ID',
    singer_name       VARCHAR(100) NOT NULL         COMMENT '歌手名',
    total_weeks       SMALLINT     DEFAULT 0        COMMENT '累计上榜总周数',
    history_best_rank TINYINT                       COMMENT '历史最高排名',
    first_chart_date  DATE                          COMMENT '首次上榜日期（关联期数 start_date）',
    last_chart_date   DATE                          COMMENT '最近上榜日期',
    champion_weeks    SMALLINT     DEFAULT 0        COMMENT '夺冠周数',
    cover_image       VARCHAR(500)                  COMMENT '封面图 URL',
    updated_at        DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_total_weeks (total_weeks DESC),
    INDEX idx_singer_id   (singer_id)
) COMMENT '歌曲留榜生命力统计表';


-- ================================================================
-- 【聚合表3】歌手全量上榜统计表
-- 由 fact_weekly_chart 聚合生成，支撑 ①饼图 ②词云 ④条形图
-- ================================================================
CREATE TABLE agg_singer_total (
    singer_id         VARCHAR(20)  PRIMARY KEY    COMMENT '歌手ID',
    singer_name       VARCHAR(100) NOT NULL        COMMENT '歌手名',
    total_count       INT          DEFAULT 0       COMMENT '历史累计上榜总周次',
    champion_count    SMALLINT     DEFAULT 0       COMMENT '历史夺冠次数',
    top3_count        SMALLINT     DEFAULT 0       COMMENT '历史进入 Top3 次数',
    distinct_songs    SMALLINT     DEFAULT 0       COMMENT '上榜不重复歌曲数',
    best_rank         TINYINT                      COMMENT '历史最好排名',
    active_years      TINYINT                      COMMENT '活跃年份数',
    first_chart_year  SMALLINT                     COMMENT '首次上榜年份',
    last_chart_year   SMALLINT                     COMMENT '最近上榜年份',
    updated_at        DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_total_count (total_count DESC)
) COMMENT '歌手全量上榜统计聚合表';
```

---

## 第二步：实现 `config.py`

`config.py` 统一管理所有配置，其他模块从这里 import，不允许硬编码。

```python
# crawler/config.py
# 需要实现以下所有配置项

import os

# ── 路径配置 ───────────────────────────────────────────────────
BASE_DIR     = os.path.dirname(os.path.abspath(__file__))
STATICS_DIR  = os.path.join(BASE_DIR, "statics")

# 三个 JSON 数据文件的绝对路径
RESULT_DATA_PATH       = os.path.join(STATICS_DIR, "result_data.json")
YEARS_ISSUE_DATA_PATH  = os.path.join(STATICS_DIR, "years_issue_data.json")
LATEST_DATA_PATH       = os.path.join(STATICS_DIR, "latest_data.json")

# ── 数据库配置 ─────────────────────────────────────────────────
DB_CONFIG = {
    "host":     "localhost",
    "port":     3306,
    "user":     "root",
    "password": "your_password",   # ← 修改为实际密码
    "database": "music_chart",
    "charset":  "utf8mb4",
    "autocommit": False,
}

# ── 定时任务配置 ───────────────────────────────────────────────
# 每天几点执行日榜抓取（24小时制）
DAILY_CRAWL_HOUR   = 8
DAILY_CRAWL_MINUTE = 0
```

---

## 第三步：实现 `models.py`

使用 Python `dataclass` 定义所有数据结构，用于在模块间传递数据，不做 ORM 映射。

```python
# crawler/models.py
# 需要实现以下所有 dataclass

from dataclasses import dataclass, field
from typing import Optional, List
from datetime import date, datetime


@dataclass
class Singer:
    """对应 dim_singer 表"""
    singer_id:   str
    singer_name: str


@dataclass
class WeekIssue:
    """对应 dim_week_issue 表
    从 years_issue_data.json 中每个 issue 对象解析而来
    """
    issue:        str
    year:         int
    month:        int
    start_date:   date
    end_date:     date
    title:        Optional[str] = None
    publish_time: Optional[datetime] = None


@dataclass
class WeeklyClassifyIndex:
    """对应 fact_weekly_classify_index 表
    从 result_data.json 每首歌的 classifyIndices 数组解析而来
    weekly_id 在入库后由数据库自增 ID 回填
    """
    weekly_id:   int   # 入库后回填
    issue:       str
    song_id:     str
    index_code:  int
    index_name:  str
    index_value: Optional[float] = None
    percentage:  Optional[int]   = None
    is_champion: int = 0


@dataclass
class WeeklyChart:
    """对应 fact_weekly_chart 表
    从 result_data.json 中 {year: {issue: [song]}} 解析而来
    indices 字段存放清洗后的 WeeklyClassifyIndex 列表，入库时分别写两张表
    """
    issue:           str
    year:            int
    rank:            int
    last_week_rank:  Optional[int]
    song_id:         str
    song_name:       str
    singer_id:       str
    singer_name:     str
    uni_index:       Optional[float] = None
    on_chart_weeks:  Optional[int]   = None
    highest_rank:    Optional[int]   = None
    history_highest: Optional[int]   = None
    new_flag:        int = 0
    cover_image:     Optional[str]   = None
    indices:         List[WeeklyClassifyIndex] = field(default_factory=list)


@dataclass
class DailyChart:
    """对应 fact_daily_chart 表
    从 latest_data.json 数组中每个对象解析而来
    chart_date 由调用方传入（date.today()）
    """
    chart_date:    date
    issue:         str
    rank:          int
    pre_rank:      Optional[int]
    incr_rank:     Optional[int]
    song_id:       str
    song_name:     str
    singer_id:     Optional[str]
    singer_name:   str
    score:         float
    pre_score:     Optional[float]
    incr_score:    Optional[float]
    play_index:    Optional[float]
    pay_index:     Optional[float]
    pop_index:     Optional[float]
    days_on_chart: Optional[int]
    high_rank:     Optional[int]
    high_index:    Optional[float]
    new_flag:      int = 0
    first_on_chart:int = 0
    track_tags:    Optional[List[str]] = None
    cover_image:   Optional[str]       = None
    publish_time:  Optional[datetime]  = None
```

---

## 第四步：实现 `cleaner.py`

负责将原始 JSON 数据解析并转换为 `models.py` 中的 dataclass 对象。所有类型转换、空值处理都在这里完成，不做任何数据库操作。

```python
# crawler/cleaner.py
# 需要实现以下所有函数

import json
from datetime import date, datetime
from typing import List, Dict, Any, Optional

from models import Singer, WeekIssue, WeeklyChart, WeeklyClassifyIndex, DailyChart


# ── 工具函数 ───────────────────────────────────────────────────

def to_float(val) -> Optional[float]:
    """将可能为字符串、None 的值转为 float，失败返回 None"""
    # 实现：try float(val)，异常或 val 为 None 时返回 None

def to_int(val) -> Optional[int]:
    """将可能为字符串、None 的值转为 int，失败返回 None"""

def to_bool_int(val) -> int:
    """将 bool/int/None 转为 0 或 1"""
    # True/"true"/1 → 1，其余 → 0

def parse_datetime(val: Optional[str]) -> Optional[datetime]:
    """解析 '2026-02-17 00:00:01' 格式字符串为 datetime，None 或空串返回 None"""

def parse_date(val: Optional[str]) -> Optional[date]:
    """解析 '2025-12-29 00:00:00' 格式字符串为 date，取日期部分"""


# ── 主解析函数 ─────────────────────────────────────────────────

def parse_years_issue(data: Dict) -> List[WeekIssue]:
    """
    解析 years_issue_data.json 的完整内容
    
    输入格式：
    {
      "2025": [
        {
          "issue": "202552",
          "month": 12,
          "title": "第52期（12.29-01.04）",
          "startTime": "2025-12-29 00:00:00",
          "endTime": "2026-01-05 00:00:00",
          "publishTime": "2026-01-05 00:22:29",
          ...
        }, ...
      ], ...
    }
    
    返回：List[WeekIssue]
    注意：year 从 issue 前4位截取（int(issue[:4])），不从外层 key 取
    """

def parse_weekly_chart(data: Dict) -> tuple[List[Singer], List[WeeklyChart]]:
    """
    解析 result_data.json 的完整内容
    
    输入格式：
    {
      "2025": {
        "202552": [
          {
            "rank": 1,
            "songId": "628126507",
            "songName": "等光来",
            "singerId": "199509",
            "singerName": "周深",
            "uniIndex": "97.04",          ← 字符串，需转 float
            "onChartWeeks": 2,
            "highestRank": 1,
            "historyHighestRank": 1,
            "newFlag": false,
            "lastWeekRank": 4,            ← 0 表示新入榜
            "coverImages": "https://...", ← 注意是 coverImages 不是 hdCoverImages
            "classifyIndices": [
              {
                "code": 1,
                "name": "播放热度",
                "index": "76.01",         ← 字符串，需转 float
                "percentage": 35,
                "isChampion": false,
              }, ...（共5个）
            ]
          }, ...
        ]
      }
    }
    
    返回：
    - singers: 去重后的 List[Singer]（用 set 对 singer_id 去重）
    - charts:  List[WeeklyChart]（每个 WeeklyChart.indices 已填充好）
    
    注意：WeeklyClassifyIndex.weekly_id 此时填 0，入库后由 db.py 回填
    """

def parse_daily_chart(data: List[Dict], chart_date: date) -> tuple[List[Singer], List[DailyChart]]:
    """
    解析 latest_data.json 的完整内容
    
    输入格式（数组，每个元素结构）：
    {
      "issue": "202667",
      "rank": 1,
      "preRank": 1,
      "incrRank": 0,
      "itemId": "467620041",              ← 对应 song_id
      "score": "96.98",                   ← 字符串，需转 float
      "preScore": "96.95",
      "incrScore": "0.03",
      "firstOnChart": 0,
      "trackTags": ["夺冠14天", "五连冠"],  ← 可能为 null
      "info": {
        "trackNameShow": "吉量",           ← 对应 song_name
        "singerId": "198658",             ← 对应 singer_id（用此字段，非 qySingerId）
        "singerName": "周深",
        "coverImages": "https://...",
        "publishTime": "2026-02-17 00:00:01"
      },
      "extend": {
        "playIndex": "75.35",             ← 字符串，需转 float
        "payIndex": "84.94",
        "popIndex": "90.22",
        "days": "19",                     ← 字符串，需转 int
        "highRank": "1",                  ← 字符串，需转 int
        "highIndex": "98.21",
        "newFlag": false
      }
    }
    
    返回：
    - singers: 去重后的 List[Singer]
    - charts:  List[DailyChart]
    
    注意：chart_date 由调用方传入，直接赋值给每条记录
    """


# ── 文件加载入口 ───────────────────────────────────────────────

def load_and_parse_years_issue(filepath: str) -> List[WeekIssue]:
    """读取 years_issue_data.json 文件并解析，返回 List[WeekIssue]"""

def load_and_parse_weekly(filepath: str) -> tuple[List[Singer], List[WeeklyChart]]:
    """读取 result_data.json 文件并解析，返回 (singers, charts)"""

def load_and_parse_daily(filepath: str, chart_date: date) -> tuple[List[Singer], List[DailyChart]]:
    """读取 latest_data.json 文件并解析，返回 (singers, charts)"""
```

---

## 第五步：实现 `db.py`

负责所有数据库的增删改查操作，使用 `pymysql`，统一通过连接池或单连接管理，对上层屏蔽 SQL 细节。

```python
# crawler/db.py
# 需要实现以下所有函数

import json
import pymysql
from typing import List
from datetime import date

from config import DB_CONFIG
from models import Singer, WeekIssue, WeeklyChart, DailyChart


# ── 连接管理 ───────────────────────────────────────────────────

def get_connection() -> pymysql.Connection:
    """
    创建并返回一个 pymysql 连接
    使用 DB_CONFIG 中的配置
    游标类型使用 pymysql.cursors.DictCursor
    """

def close_connection(conn: pymysql.Connection):
    """安全关闭连接"""


# ── 维度表写入 ─────────────────────────────────────────────────

def upsert_singers(conn, singers: List[Singer]):
    """
    批量写入歌手维度表
    SQL：INSERT INTO dim_singer (singer_id, singer_name)
         VALUES (%s, %s)
         ON DUPLICATE KEY UPDATE singer_name = VALUES(singer_name)
    使用 executemany 批量执行，最后 commit
    """

def upsert_week_issues(conn, issues: List[WeekIssue]):
    """
    批量写入期数元数据表
    SQL：INSERT INTO dim_week_issue (issue, year, month, start_date, end_date, title, publish_time)
         VALUES (...)
         ON DUPLICATE KEY UPDATE
           title = VALUES(title),
           publish_time = VALUES(publish_time)
    使用 executemany 批量执行，最后 commit
    """


# ── 历史周榜写入 ───────────────────────────────────────────────

def insert_weekly_charts(conn, charts: List[WeeklyChart]):
    """
    批量写入历史周榜明细（fact_weekly_chart）及对应的五维指数（fact_weekly_classify_index）
    
    执行步骤：
    1. 对每条 WeeklyChart，INSERT INTO fact_weekly_chart (...) VALUES (...)
       使用 INSERT IGNORE（跳过重复，避免重复执行时报错）
    2. 获取刚插入行的 lastrowid，回填给该 chart 的每个 index.weekly_id
    3. 如果 chart.indices 非空，INSERT INTO fact_weekly_classify_index (...) VALUES (...)
    4. 每 500 条 commit 一次，避免事务过大
    5. 最后再 commit 一次
    
    注意：INSERT IGNORE 在主键冲突时不会抛错，lastrowid 为 0，此时跳过指数写入
    """

def is_weekly_data_exists(conn) -> bool:
    """
    检查 fact_weekly_chart 表是否已有数据
    SELECT COUNT(*) FROM fact_weekly_chart LIMIT 1
    返回 True/False，用于 pipeline 判断是否需要全量导入
    """


# ── 日榜写入 ───────────────────────────────────────────────────

def upsert_daily_charts(conn, charts: List[DailyChart]):
    """
    批量写入/更新日榜数据（fact_daily_chart）
    
    SQL：INSERT INTO fact_daily_chart (chart_date, issue, rank, ..., track_tags, ...)
         VALUES (...)
         ON DUPLICATE KEY UPDATE
           score = VALUES(score),
           incr_rank = VALUES(incr_rank),
           incr_score = VALUES(incr_score),
           play_index = VALUES(play_index),
           pay_index = VALUES(pay_index),
           pop_index = VALUES(pop_index),
           track_tags = VALUES(track_tags)
    
    注意：
    - track_tags 字段入库前需先 json.dumps(chart.track_tags) 转字符串
    - publish_time 为 None 时直接传 None
    - 唯一键为 (chart_date, rank)，同一天重复爬取时更新得分相关字段
    - 使用 executemany 批量执行，最后 commit
    """

def get_latest_daily_date(conn) -> date | None:
    """
    查询 fact_daily_chart 中最近一天的日期
    SELECT MAX(chart_date) FROM fact_daily_chart
    返回 date 对象或 None
    """


# ── 聚合表刷新 ─────────────────────────────────────────────────

def refresh_agg_singer_yearly(conn):
    """
    刷新歌手历年上榜统计聚合表（agg_singer_yearly）
    
    执行以下 SQL（先删后插策略，保证幂等）：
    
    INSERT INTO agg_singer_yearly
      (singer_id, singer_name, year, chart_count, top3_count, champion_count, avg_rank, avg_index)
    SELECT
      singer_id,
      singer_name,
      year,
      COUNT(*)                                          AS chart_count,
      SUM(CASE WHEN rank <= 3 THEN 1 ELSE 0 END)       AS top3_count,
      SUM(CASE WHEN rank = 1  THEN 1 ELSE 0 END)       AS champion_count,
      ROUND(AVG(rank), 2)                               AS avg_rank,
      ROUND(AVG(uni_index), 2)                          AS avg_index
    FROM fact_weekly_chart
    GROUP BY singer_id, singer_name, year
    ON DUPLICATE KEY UPDATE
      chart_count    = VALUES(chart_count),
      top3_count     = VALUES(top3_count),
      champion_count = VALUES(champion_count),
      avg_rank       = VALUES(avg_rank),
      avg_index      = VALUES(avg_index)
    
    最后 commit
    """

def refresh_agg_song_longevity(conn):
    """
    刷新歌曲留榜生命力聚合表（agg_song_longevity）
    
    执行以下 SQL：
    
    INSERT INTO agg_song_longevity
      (song_id, song_name, singer_id, singer_name,
       total_weeks, history_best_rank, first_chart_date, last_chart_date, champion_weeks, cover_image)
    SELECT
      f.song_id,
      f.song_name,
      f.singer_id,
      f.singer_name,
      COUNT(*)                                          AS total_weeks,
      MIN(f.rank)                                       AS history_best_rank,
      MIN(i.start_date)                                 AS first_chart_date,
      MAX(i.end_date)                                   AS last_chart_date,
      SUM(CASE WHEN f.rank = 1 THEN 1 ELSE 0 END)      AS champion_weeks,
      MAX(f.cover_image)                                AS cover_image
    FROM fact_weekly_chart f
    LEFT JOIN dim_week_issue i ON f.issue = i.issue
    GROUP BY f.song_id, f.song_name, f.singer_id, f.singer_name
    ON DUPLICATE KEY UPDATE
      total_weeks       = VALUES(total_weeks),
      history_best_rank = VALUES(history_best_rank),
      first_chart_date  = VALUES(first_chart_date),
      last_chart_date   = VALUES(last_chart_date),
      champion_weeks    = VALUES(champion_weeks),
      cover_image       = VALUES(cover_image)
    
    最后 commit
    """

def refresh_agg_singer_total(conn):
    """
    刷新歌手全量统计聚合表（agg_singer_total）
    
    执行以下 SQL：
    
    INSERT INTO agg_singer_total
      (singer_id, singer_name, total_count, champion_count, top3_count,
       distinct_songs, best_rank, active_years, first_chart_year, last_chart_year)
    SELECT
      singer_id,
      singer_name,
      COUNT(*)                                          AS total_count,
      SUM(CASE WHEN rank = 1 THEN 1 ELSE 0 END)        AS champion_count,
      SUM(CASE WHEN rank <= 3 THEN 1 ELSE 0 END)       AS top3_count,
      COUNT(DISTINCT song_id)                           AS distinct_songs,
      MIN(rank)                                         AS best_rank,
      COUNT(DISTINCT year)                              AS active_years,
      MIN(year)                                         AS first_chart_year,
      MAX(year)                                         AS last_chart_year
    FROM fact_weekly_chart
    GROUP BY singer_id, singer_name
    ON DUPLICATE KEY UPDATE
      total_count    = VALUES(total_count),
      champion_count = VALUES(champion_count),
      top3_count     = VALUES(top3_count),
      distinct_songs = VALUES(distinct_songs),
      best_rank      = VALUES(best_rank),
      active_years   = VALUES(active_years),
      first_chart_year = VALUES(first_chart_year),
      last_chart_year  = VALUES(last_chart_year)
    
    最后 commit
    """

def refresh_all_agg(conn):
    """
    依次调用三个聚合刷新函数的入口
    顺序：refresh_agg_singer_yearly → refresh_agg_song_longevity → refresh_agg_singer_total
    每个函数执行完后打印日志
    """
```

---

## 第六步：实现 `pipeline.py`

将 cleaner + db 串联成完整的数据流水线，对外暴露两个入口函数。

```python
# crawler/pipeline.py
# 需要实现以下所有函数

from datetime import date

from config import RESULT_DATA_PATH, YEARS_ISSUE_DATA_PATH, LATEST_DATA_PATH
import cleaner
import db


def run_history_pipeline():
    """
    历史数据全量导入流水线（只需执行一次）
    
    执行步骤：
    1. 调用 db.get_connection() 获取连接
    2. 调用 db.is_weekly_data_exists(conn)，若已有数据则打印提示并 return（防止重复导入）
    3. 调用 cleaner.load_and_parse_years_issue(YEARS_ISSUE_DATA_PATH)，得到 issues
    4. 调用 db.upsert_week_issues(conn, issues)，写入期数元数据
    5. 调用 cleaner.load_and_parse_weekly(RESULT_DATA_PATH)，得到 (singers, charts)
    6. 调用 db.upsert_singers(conn, singers)，写入歌手维度
    7. 调用 db.insert_weekly_charts(conn, charts)，写入周榜明细和指数（最耗时）
    8. 调用 db.refresh_all_agg(conn)，刷新三张聚合表
    9. 关闭连接
    10. 全程打印进度日志，每步完成后输出耗时
    """

def run_daily_pipeline():
    """
    日榜数据每日增量更新流水线（由 scheduler 每天调用）
    
    执行步骤：
    1. 调用 db.get_connection() 获取连接
    2. 调用 cleaner.load_and_parse_daily(LATEST_DATA_PATH, date.today())，得到 (singers, charts)
    3. 调用 db.upsert_singers(conn, singers)，补充新歌手
    4. 调用 db.upsert_daily_charts(conn, charts)，写入日榜数据
    5. 关闭连接
    6. 打印完成日志（写入条数、日期）
    
    注意：此流水线不刷新聚合表（聚合表只依赖周榜）
    """
```

---

## 第七步：实现 `scheduler.py`

使用 `APScheduler` 实现每日定时任务，每天固定时刻自动爬取日榜并入库。

```python
# crawler/scheduler.py
# 需要实现定时任务

# 依赖：pip install apscheduler

from apscheduler.schedulers.blocking import BlockingScheduler
from config import DAILY_CRAWL_HOUR, DAILY_CRAWL_MINUTE


def daily_job():
    """
    定时任务执行体，每天触发一次
    
    执行步骤（顺序不可乱）：
    1. 打印开始日志（含当前时间）
    2. 启动 DrissionPage Chromium，调用 spider.DataFetcher.get_latest_rank()
       ← 此步骤会将结果写入 statics/latest_data.json
    3. 关闭浏览器
    4. 调用 pipeline.run_daily_pipeline()，将 latest_data.json 入库
    5. 打印完成日志
    6. 任何异常均捕获并打印，不中断 scheduler 进程
    """

def start():
    """
    启动 BlockingScheduler
    添加 cron 触发器：hour=DAILY_CRAWL_HOUR, minute=DAILY_CRAWL_MINUTE
    打印启动日志（含执行时间配置）
    调用 scheduler.start()（阻塞进程）
    """
```

---

## 第八步：实现 `main.py`

统一命令行入口，支持通过参数选择执行模式。

```python
# crawler/main.py
# 需要实现命令行入口

# 支持以下三种运行模式（通过命令行参数区分）：
#
# python main.py history   → 执行历史数据全量导入（只需运行一次）
# python main.py daily     → 立即执行一次日榜爬取+入库（手动触发，测试用）
# python main.py schedule  → 启动定时调度器（生产运行，常驻进程）
#
# 实现要求：
# - 使用 sys.argv[1] 读取模式参数
# - 无参数或参数错误时打印使用说明并退出
# - 每种模式调用对应的函数：
#     history  → pipeline.run_history_pipeline()
#     daily    → spider 抓取 + pipeline.run_daily_pipeline()
#     schedule → scheduler.start()
```

---

## 第九步：运行顺序

按以下顺序执行，**不可跳步**：

```bash
# 1. 安装依赖
pip install pymysql apscheduler DrissionPage

# 2. 修改 config.py 中的数据库密码

# 3. 在 MySQL 中执行第一步的全部建表 SQL

# 4. 确认 result_data.json 和 years_issue_data.json 已在 statics/ 目录下

# 5. 执行历史数据全量导入（仅此一次，耗时较长）
python main.py history

# 6. 手动测试日榜抓取+入库
python main.py daily

# 7. 启动每日定时调度（生产环境常驻运行）
python main.py schedule
```

---

## 附录：字段映射速查表

### result_data.json → fact_weekly_chart

| JSON 字段 | 数据库字段 | 类型转换 |
|-----------|-----------|---------|
| `rank` | `rank` | 直接写入 |
| `lastWeekRank` | `last_week_rank` | 直接写入，可为 0 |
| `songId` | `song_id` | 字符串 |
| `songName` | `song_name` | 字符串 |
| `singerId` | `singer_id` | 字符串 |
| `singerName` | `singer_name` | 字符串 |
| `uniIndex` | `uni_index` | `to_float()` |
| `onChartWeeks` | `on_chart_weeks` | 直接写入 |
| `highestRank` | `highest_rank` | 直接写入 |
| `historyHighestRank` | `history_highest` | 直接写入 |
| `newFlag` | `new_flag` | `to_bool_int()` |
| `coverImages` | `cover_image` | 字符串 |
| 外层 year key | `year` | `int()` |
| 外层 issue key | `issue` | 字符串 |

### latest_data.json → fact_daily_chart

| JSON 路径 | 数据库字段 | 类型转换 |
|-----------|-----------|---------|
| `issue` | `issue` | 字符串 |
| `rank` | `rank` | 直接写入 |
| `preRank` | `pre_rank` | 直接写入 |
| `incrRank` | `incr_rank` | 直接写入 |
| `itemId` | `song_id` | 字符串 |
| `info.trackNameShow` | `song_name` | 字符串 |
| `info.singerId` | `singer_id` | 字符串 |
| `info.singerName` | `singer_name` | 字符串 |
| `score` | `score` | `to_float()` |
| `preScore` | `pre_score` | `to_float()` |
| `incrScore` | `incr_score` | `to_float()` |
| `extend.playIndex` | `play_index` | `to_float()` |
| `extend.payIndex` | `pay_index` | `to_float()` |
| `extend.popIndex` | `pop_index` | `to_float()` |
| `extend.days` | `days_on_chart` | `to_int()` |
| `extend.highRank` | `high_rank` | `to_int()` |
| `extend.highIndex` | `high_index` | `to_float()` |
| `extend.newFlag` | `new_flag` | `to_bool_int()` |
| `firstOnChart` | `first_on_chart` | `to_bool_int()` |
| `trackTags` | `track_tags` | `json.dumps()`，None→None |
| `info.coverImages` | `cover_image` | 字符串 |
| `info.publishTime` | `publish_time` | `parse_datetime()` |
| 调用方传入 | `chart_date` | `date.today()` |
